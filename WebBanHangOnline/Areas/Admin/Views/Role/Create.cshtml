@using Microsoft.AspNet.Identity.EntityFramework
@model IdentityRole
@{
    ViewBag.Title = "Thêm mới Quyền";
}
<!-- Content -->

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light"><a href="/admin">Trang chủ</a> /</span> @ViewBag.Title</h4>

    <!-- Basic Layout -->

    @using (Html.BeginForm("Create", "Role", FormMethod.Post, new { id = "form-add" }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(false, "", new { @class = "text-danger" })

        <div class="row">
            <div class="col-xl">
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">@ViewBag.Title</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label class="form-label">Tên quyền</label>
                            @Html.TextBoxFor(x => x.Name, new { @class = "form-control", placeholder = "Tên quyền", required = "required", @data_parsley_required_message = "Không được để trống" })
                        </div>
                        <div class="mb-3">
                            <label class="form-label">ID quyền</label>
                            @Html.TextBoxFor(x => x.Id, new { @class = "form-control", placeholder = "ID quyền", required = "required", @data_parsley_required_message = "Không được để trống" })
                        </div>
                        <!-- Nút submit -->
                        <div class="card-body">
                            <div class="demo-inline-spacing">
                                <a href="/admin/role" type="button" class="btn rounded-pill btn-outline-secondary">Trở lại</a>
                                <button type="submit" class="btn rounded-pill btn-outline-success">Thêm mới</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
<!-- / Content -->

@section scripts {
    <script>
        $(document).ready(function () {
            $('#form-add').parsley(); // Khởi động parsley
        });
    </script>
    <script>
        $('form').on('submit', function (e) {
            console.log('Form is submitting');
        });
    </script>
}